# Uploaded receipt images only - never execute anything here as PHP,
# and don't let visitors browse the directory listing.
<IfModule mod_php.c>
  php_flag engine off
</IfModule>
<FilesMatch "\.(php|phtml|php\d)$">
  Require all denied
</FilesMatch>
Options -Indexes
